#include "main.h"

/**
 * print_percent - Handles the '%' conversion specifier.
 * @args: Argument list (not used for '%').
 *
 * Return: The number of characters printed.
 */
int print3(void)
{
    UNUSED(args);
    _putchar('%');
    return (1);
}
